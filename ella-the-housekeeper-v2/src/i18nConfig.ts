const i18nConfig: {
  locales: string[];
  defaultLocale: string;
  prefixDefault: boolean;
} = {
  locales: ["en", "pt-BR", "de", "fr"],
  defaultLocale: "en",
  prefixDefault: true,
};

export default i18nConfig; //this is the default export for the file
